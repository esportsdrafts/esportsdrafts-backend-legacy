apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-nginx
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/tls-acme: "true"
    ingress.kubernetes.io/force-ssl-redirect: "true"
    ingress.kubernetes.io/enable-modsecurity: "true"
    ingress.kubernetes.io/enable-owasp-core-rules: "true"
    ingress.kubernetes.io/enable-cors: "true"
    ingress.kubernetes.io/cors-allow-origin: "https://efantasy.dev"
    ingress.kubernetes.io/cors-allow-credentials: "true"
spec:
  tls:
    - hosts:
      - api.efantasy.dev
      - efantasy.dev
      secretName: tls-certificate
  rules:
  - host: api.efantasy.dev
    http:
      paths:
      - path: /v1/auth
        backend:
          serviceName: auth
          servicePort: 8000
#     - path: /v1/games
#       backend:
#         serviceName: games
#         servicePort: 8000
#     - path: /v1/users
#       backend:
#         serviceName: users
#         servicePort: 8000
  - host: efantasy.dev
    http:
      paths:
      - path: /
        backend:
          serviceName: frontend
          servicePort: 8000
